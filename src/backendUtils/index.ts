import BackendHelper from './helper';
import BackendExtensions from './extensions';

export default class BackendUtils {
  static BackendExtensions = BackendExtensions;
  static BackendHelper = BackendHelper;
}
