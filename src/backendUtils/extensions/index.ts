import BackendHooks from "./hooks";
import BackendEndpoints from "./endpoints";

export default class BackendExtensions {
  static BackendHooks = BackendHooks;
  static BackendEndpoints = BackendEndpoints;
}
